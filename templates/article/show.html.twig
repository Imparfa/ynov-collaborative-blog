{# templates/article/show.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}{{ article.title }}{% endblock %}

{% block body %}
    <div class="article-detail">
        <h1>{{ article.title }}</h1>
        <p>{{ article.content }}</p>
        <a href="{{ path('article_index') }}" class="btn">Back to list</a>
    </div>
{% endblock %}
